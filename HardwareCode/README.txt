NOTE: to run the contiki code, please follow the tutorial on our documentation website. 

To run the arduino code, simply select the serial port of the arduino and run it.

To run the Java code, run it in eclipse making sure to specify the COM under run configurations. (you may need to install the RXTX libraries on your computer to get it to work)